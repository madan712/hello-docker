version: '3'

services:
  mysqldb:
    image: mariadb
    container_name: mysql
    platform: linux/amd64 # enable this for linux/mac
    restart: always
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=pass123
      - MYSQL_DATABASE=mydb
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - mynet

  adminer:
    image: adminer
    container_name: adminer
    restart: always
    ports:
      - 8081:8080
    networks:
      - mynet

  hello-docker:
    depends_on:
      - mysqldb
    image: madan712/hello-docker:v3.0
    container_name: hello-docker
    restart: on-failure
    ports:
      - 8080:8080
    environment:
      SPRING_APPLICATION_JSON: '{
          "spring.datasource.url": "jdbc:mysql://mysqldb:3306/mydb?allowPublicKeyRetrieval=true&useSSL=false",
          "spring.datasource.username":"root",
          "spring.datasource.password":"pass123"
        }'
    networks:
      - mynet

volumes:
  mysql-data:

networks:
  mynet:
